import uuid

def call_nano_banana(base_image_url, prompt):
    # 這只是範例，實際請根據 Nano Banana 的 API 文件修改
    # nano_api_url = "http://nano-banana-service/generate"
    # response = requests.post(nano_api_url, json={"image": base_image_url, "prompt": prompt})
    # return response.json().get('url')
    return f"http://placeholder-ai-result.com/{uuid.uuid4()}.jpg" # 測試用